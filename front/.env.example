VITE_API_URL=http://localhost:8000
# VITE_REGISTRATION_ENABLED → défini via REGISTRATION_ENABLED dans le .env racine (Docker Compose l'injecte au build)
# Pour un déploiement sans Docker, ajouter : VITE_REGISTRATION_ENABLED=true
